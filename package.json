{
  "name": "wbs-ingestion",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "lint": "eslint src/**/*.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240405.0",
    "typescript": "^5.0.4",
    "vitest": "^1.5.0",
    "wrangler": "^4.53.0"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.30",
    "mongodb": "^6.5.0"
  }
}