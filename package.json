{
  "name": "wbs-ingestion-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build:all": "turbo run build",
    "dev:all": "turbo run dev",
    "lint:all": "turbo run lint",
    "test:all": "turbo run test",
    "sync": "cd apps/site && rm -rf .angular && npx syncfusion-license activate"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.4",
    "prettier": "^3.6.2",
    "turbo": "^2.5.6"
  },
  "packageManager": "pnpm@10.24.0",
  "engines": {
    "node": ">=24.0.0"
  }
}